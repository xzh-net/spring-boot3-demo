# ============================================================
# 开发环境 (dev) — 团队共享开发环境
# ============================================================
# 激活方式: 默认 (application.yml 中 spring.profiles.active=dev)
#           或命令行: --spring.profiles.active=dev
#
# dev 与 local 的 OAuth2 端点地址均为 localhost:9000,
# 使用 application.yml 中的默认配置即可, 无需额外覆盖.

# (无环境特定覆盖项)
